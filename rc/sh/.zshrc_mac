alias ls="ls -G"
alias ll="ls -al"

export PATH=$HOME/DevEnv/bin/slink:$PATH

alias ctags="`brew --prefix`/bin/ctags --exclude={.metadata,.svn,.git}"

alias grep="grep --color=auto"

alias find="find -L"
