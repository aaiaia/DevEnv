#!/bin/sh
echo '============================================================'
echo 'Initializing... on "'$SHELL'"'
#current_path=`pwd -P`
current_path=`pwd -P`
echo 'Current path is "'$current_path'"'

cd ~/DevEnv
echo 'Move to "~/DevEnv"'

echo 'Update pleya developments environments using git'
echo '$ git pull'
git pull

#cd $current_path
#echo 'Move to "'$current_path'"'
cd ~/
echo 'Move to ~/'
echo 'Updating environments done'
echo '============================================================'
echo ''
