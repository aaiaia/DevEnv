#!/bin/sh

version="find variables for project folder"

usage="Usage: $0 [VARIABLE NAME]"

find ./ -type f \( -iname "*" ! -iname ".svn" ! -iname "*.bak" ! -iname "tags" \) | xargs grep -n -w --text --color=auto $1

